[project]
name = "retail-api"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "django>=5.0.0",
    "djangorestframework>=3.15.0",
    "psycopg[binary]>=3.1.0",
]

[tool.setuptools]
packages = ["core"]
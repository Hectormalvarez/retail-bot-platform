services:
  bot:
    build:
      context: ./bot
    container_name: retail_bot
    restart: unless-stopped
    env_file:
      - .env
    networks:
      - retail_mesh

networks:
  retail_mesh:
    driver: bridge